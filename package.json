{
  "name": "pixelforge",
  "private": true,
  "version": "1.0.0",
  "description": "Freepik-style multi-model AI image generator powered by the Agnic AI Gateway.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,magenta \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w frontend",
    "start": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
